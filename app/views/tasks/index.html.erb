<h1>All Tasks</h1>

<p><%= link_to 'Add Task', new_task_path %></p>

<p>Showing <%= @tasks.count %> tasks</p>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %> </li>
  <% end %>
</ul>
